---
# nut role defaults
# All values (nut_enabled, nut_ups_name, nut_ups_driver, nut_ups_port,
# nut_monitor_user) are defined in ansible/vars/main.yml.
# Override here only for non-standard UPS configurations.
