[Unit]
# Stop while the host network and QEMU's SSH forward remain available.
After=network.target

[Service]
ExecStop=
ExecStop=/usr/local/sbin/construct-stop $MAINPID
# Helper waits at most 120s; systemd retains bounded termination fallback.
TimeoutStopSec=135s
