---
# mirror-hdd role defaults
# All values (mirror_source, mirror_dest, mirror_on_calendar, mirror_log)
# are defined in ansible/vars/main.yml.
# Override here only if you need a non-standard mirror on a specific host.
