# Omada reconcile runs locally on the watchtower runner and talks to the
# OC200 over HTTPS. No SSH target — connection is local; the API calls
# in the role reach out to omada_api_base.
[omada_runner]
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3
