apiVersion: apps/v1
kind: Deployment
metadata:
  name: navidrome
  namespace: navidrome
spec:
  replicas: 1
  selector:
    matchLabels:
      app: navidrome
  template:
    metadata:
      labels:
        app: navidrome
    spec:
      containers:
        - name: navidrome
          image: deluan/navidrome:0.53.3  # <-- Change from :latest to a specific version
          envFrom:
            - configMapRef:
                name: navidrome-config
          ports:
            - containerPort: 4533
          volumeMounts:
            - name: db-storage
              mountPath: /data
            - name: music-storage
              mountPath: /music
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
      volumes:
        - name: db-storage
          persistentVolumeClaim:
            claimName: navidrome-db
        - name: music-storage
          hostPath:
            path: /mnt/hdd-c/music-library  # → /mnt/hdd/music-library after 8TB swap
            type: Directory
