# TODO: vault these credentials before sharing this repo publicly
# Run: ansible-vault encrypt services/fileserver/ansible/group_vars/all/vault.yml
# Then move samba_user and samba_password into vault.yml and reference here

samba_user: vault
samba_password: "bubbie"      # ← replace before deploying
samba_share_name: vault
samba_share_path: /mnt/ssd-b/vault
