# All Samba variables (samba_user, james_user, share names and paths)
# are now defined in ansible/vars/main.yml.
# Secrets (vault_samba_password, vault_james_password) are in ansible/vars/vault.yml.
# This file is intentionally empty — kept for directory structure consistency.
