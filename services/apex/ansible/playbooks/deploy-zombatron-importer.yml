---
- name: Deploy Zombatron Importer
  hosts: apex
  gather_facts: true

  vars_files:
    - "{{ playbook_dir }}/../../../../ansible/vars/main.yml"
    - "{{ playbook_dir }}/../../../../ansible/vars/vault.yml"

  roles:
    - role: "{{ playbook_dir }}/../roles/zombatron_importer"
