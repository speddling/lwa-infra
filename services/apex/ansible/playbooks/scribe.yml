---
# deploy-scribe.yml
# Run with:  ansible-playbook playbooks/scribe.yml
# from:      services/apex/ansible/

- name: Deploy Scribe — Claude's git control plane
  hosts: apex
  gather_facts: true

  roles:
    - scribe
