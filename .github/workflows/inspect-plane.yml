name: Inspect Plane startup

on:
  pull_request:
    branches: [master]
    paths:
      - 'scripts/plane/**'
      - '.github/workflows/inspect-plane.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check inspection syntax without contacting production
        run: python -m py_compile scripts/plane/inspect.py

  inspect:
    needs: validate
    if: github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/master'
    runs-on: [self-hosted, monolith]
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Verify runner identity
        run: test "$(hostname -s)/$(id -un)" = monolith/gh-runner
      - name: Inspect through the existing speddling SSH login
        run: |
          ssh -o BatchMode=yes -o StrictHostKeyChecking=yes \
            -o ConnectTimeout=10 -o IdentitiesOnly=yes \
            -i "$HOME/.ssh/id_ed25519" speddling@192.168.30.10 \
            'sudo -n /usr/bin/python3 -' < scripts/plane/inspect.py
